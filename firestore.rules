rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{project} {
      allow read, write: if request.auth != null;
    }
    match /customTones/{tone} {
      allow read, write: if request.auth != null;
    }
  }
}
